({
	afterRender : function(component, helper){
       /*  var identifier =  component.get("v.identifier");
        if (document.getElementById('chart'+identifier) != null) {
            
             var canvas = document.getElementById('chart'+identifier);
         console.log(canvas+'********');
var att = document.createAttribute("aura:id");       // Create a "class" attribute
att.value = 'chart'+identifier;                           // Set the value of the class attribute
canvas.setAttributeNode(att);
        }
        else{
            console.log('Null Exists========');
        }

            this.superAfterRender();*/

}
})